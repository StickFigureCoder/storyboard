import { version } from '../../package.json';
// place files you want to import through the `$lib` alias in this folder.
export const CURRENT_VERSION = version;
export const EXTENSION = 'sboard';
export const WORLD_FILE = 'world.json';
